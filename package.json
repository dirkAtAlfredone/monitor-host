{
  "name": "ping-host",
  "version": "1.0.0",
  "description": "Script to ping servers and send email to IT staff if it fails.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dirk Brandon Lapitan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "nodemailer": "^6.10.1",
    "ping": "^0.4.4"
  }
}
